<?php

namespace Deveative\EasyForm\Validate\Message;

class English {

    const Required = ':label is invalid(required)';

    const Maxlength = ':label is invalid(maxlength :length characters)';

}